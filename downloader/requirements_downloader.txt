# Requirements for download_attachments.py utility

requests>=2.31.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Text conversion dependencies (for --convert-text flag and text_converter.py)
pdfplumber>=0.10.0
python-docx>=1.1.0

# OCR fallback for PDFs with broken font encodings (optional but recommended)
# Also requires Tesseract OCR engine installed on the system and poppler (for pdf2image)
pdf2image>=1.16.0
pytesseract>=0.3.10
